# ── Database ──────────────────────────────────────
# Development (SQLite):
DATABASE_URL="file:./backshots.db"
# Production (PostgreSQL):
# DATABASE_URL="postgresql://backshots:STRONG_PASSWORD@db:5432/backshots?schema=public"

# ── Auth ──────────────────────────────────────────
JWT_SECRET="CHANGE-ME-to-a-random-64-char-string"

# ── Server ────────────────────────────────────────
PORT=3001
NODE_ENV=development
FRONTEND_URL="http://localhost:5173"
BASE_URL="http://localhost:3001"

# ── Storage ───────────────────────────────────────
STORAGE_TYPE=filesystem
UPLOAD_DIR="../uploads"
EXPORT_DIR="../exports"

# S3-compatible storage (uncomment for production)
# S3_BUCKET=your-bucket-name
# S3_REGION=us-east-1
# S3_PUBLIC_URL=https://your-bucket.s3.amazonaws.com
# S3_ACCESS_KEY_ID=your-access-key
# S3_SECRET_ACCESS_KEY=your-secret-key
# S3_ENDPOINT=               # optional: for R2, MinIO, DigitalOcean Spaces
# S3_FORCE_PATH_STYLE=false  # set true for MinIO
